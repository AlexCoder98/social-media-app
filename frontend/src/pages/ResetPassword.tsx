import ResetPasswordForm from "../components/Body/Forms/ResetPassword";

const ResetPasswordPage = () => {
    return (
        <div
            className="app__page"
            data-page-name="reset-password"
        >
            <ResetPasswordForm />
        </div>
    );
};

export default ResetPasswordPage;