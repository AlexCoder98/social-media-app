import '../../styles/components_styles/notifications/NoMoreData.css';

const NoMoreData = () => {
    return (
        <div className="app__no-more-data-container">
            <p className="app__no-more-data">That's all for now</p>
        </div>
    )
};

export default NoMoreData;